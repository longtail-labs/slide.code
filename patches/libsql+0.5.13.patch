diff --git a/node_modules/libsql/.DS_Store b/node_modules/libsql/.DS_Store
new file mode 100644
index 0000000..9e99537
Binary files /dev/null and b/node_modules/libsql/.DS_Store differ
diff --git a/node_modules/libsql/index.js b/node_modules/libsql/index.js
index 9ac15cc..f4850dd 100644
--- a/node_modules/libsql/index.js
+++ b/node_modules/libsql/index.js
@@ -2,6 +2,8 @@
 
 const { load, currentTarget } = require("@neon-rs/load");
 const { familySync, GLIBC, MUSL } = require("detect-libc");
+const fs = require("fs");
+const path = require("path");
 
 function requireNative() {
   if (process.env.LIBSQL_JS_DEV) {
@@ -23,6 +25,31 @@ function requireNative() {
   if (target === "linux-arm-gnueabihf" && familySync() == MUSL) {
       target = "linux-arm-musleabihf";
   }
+
+  // Check for Resources first, then bundled modules
+  try {
+    // First priority: Resources path (for packaged apps)
+    if (process.resourcesPath) {
+      // For Windows, use the msvc-specific path in Resources
+      if (target === "win32-x64") {
+        const msvcResourcePath = path.join(process.resourcesPath, "libsql", "win32-x64-msvc", "index.node");
+        if (fs.existsSync(msvcResourcePath)) {
+          const nativeModule = { exports: {} };
+          process.dlopen(nativeModule, msvcResourcePath);
+          return nativeModule.exports;
+        }
+      } else if (target === "win32-arm64") {
+        const msvcResourcePath = path.join(process.resourcesPath, "libsql", "win32-arm64-msvc", "index.node");
+        if (fs.existsSync(msvcResourcePath)) {
+          const nativeModule = { exports: {} };
+          process.dlopen(nativeModule, msvcResourcePath);
+          return nativeModule.exports;
+        }
+      } else {
+        // For other platforms, use standard target path
+        const resourcePath = path.join(process.resourcesPath, "libsql", target, "index.node");
+        if (fs.existsSync(resourcePath)) {
+          const nativeModule = { exports: {} };
+          process.dlopen(nativeModule, resourcePath);
+          return nativeModule.exports;
+        }
+      }
+    }
+    
+    // Second priority: bundled_modules path (for development)
+    const bundledPath = path.join(__dirname, "..", "..", "bundled_modules", "libsql", target, "index.node");
+    if (fs.existsSync(bundledPath)) {
+      const nativeModule = { exports: {} };
+      process.dlopen(nativeModule, bundledPath);
+      return nativeModule.exports;
+    }
+    
+    // Third priority: Check for Windows-specific msvc target in node_modules
+    if (target === "win32-x64") {
+      try {
+        return require(`@libsql/win32-x64-msvc`);
+      } catch (e) {
+        // Continue to fallback
+      }
+    } else if (target === "win32-arm64") {
+      try {
+        return require(`@libsql/win32-arm64-msvc`);
+      } catch (e) {
+        // Continue to fallback
+      }
+    }
+  } catch (e) {
+    // Ignore errors and fall back to original behavior
+  }
+
+  // Fall back to original behavior
   return require(`@libsql/${target}`);
 }
 
